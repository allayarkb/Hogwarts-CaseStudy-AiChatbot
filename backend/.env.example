# Copy this file to .env and fill in real values. Never commit .env.

# Groq API key from https://console.groq.com/ (server-side only)
GROQ_API_KEY=

# Local dev port (Railway sets PORT automatically in production)
PORT=5000
